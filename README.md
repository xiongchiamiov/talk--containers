What actually are containers?  What makes them different from virtual machines,
technologically and in how we use them?  What sorts of security concerns should
we think about when running a container?
